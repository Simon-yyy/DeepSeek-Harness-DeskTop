## v1.1.0 (2026-08-17)

### 重大升级
- ⚡ 原生 Electron utilityProcess 容器替代外部 Node.js 子进程
- 🛡️ 生命周期强绑定，退出即 100% 销毁后端进程，0 端口残留
- 🧹 启动时自动清理 Chromium 脏缓存，防止插件白屏
- 🔄 插件注册幂等容错盾，消灭 Failed to load plugins 白屏
- 🔑 全局异常边界捕获，友好错误弹窗替代静默崩溃
- 📶 Node.js PATH 自动注入，支持桌面快捷方式直接启动

# 更新历史记录 (Changelog)
## 🚀 [v1.0.2] 关键交互与文档拖拽修复版

- 🐛 **修复 React 18 受控输入框路径同步**：使用原生 Setter 穿透受控组件拦截，拖拽文件绝对路径 100% 写入输入框状态。
- 🐛 **修复全屏拖拽遮罩层偶发卡死**：完善 `dragend` 派发与 `Escape` 按键逃逸机制。
- 🛡️ **拦截非图片文件报错弹窗**：在捕获阶段处理文件拖拽，消除“仅支持图片”的误报提示。

---

所有重要的架构更新与功能演进均记录于此。

---

## 🚀 [v1.0.0 - v1.0.4] 核心全功能正式里程碑

本次大版本整合完成了从“简易网页外壳”到“现代化专业 AI 编程桌面工作台”的完整蜕变，包含以下五大核心支柱能力：

### 1. 🖥️ 原生桌面交互与系统级集成
- **独立沉浸窗口**：锁死窗口标题为 `DSH Desktop`，屏蔽网页端长 URL 污染，定制官方经典黑白鲸鱼图标。
- **单实例运行锁 (Single Instance Lock)**：防止多开冲突，重复启动自动聚焦已有窗口。
- **系统托盘与常驻 (System Tray)**：支持托盘图标点击唤醒、右键快捷菜单（显示/新建会话/检查更新/退出）。
- **全局快捷键随时召唤**：支持 `Ctrl + Shift + D`（Mac 下 `Cmd + Shift + D`）全局一秒唤出或收起。
- **高分屏字体缩放**：支持 `Ctrl +` / `Ctrl -` / `Ctrl 0` 自由缩放界面字号。

### 2. 📄 全格式文档与截图直通 (Universal Pipeline)
- **ModLens 视觉直通**：截图按下 `Ctrl + V` 自动拦截并转存为本地路径，消除“模型不支持图片附件”报错。
- **全文档格式拖拽与粘贴**：支持将 **PDF、Word (.docx/.doc)、Excel (.xlsx)、Markdown、代码文件** 直接拖入窗口或复制粘贴，自动识别并填入本地绝对路径供 AI 全文解析。

### 3. 🧩 35 个工业级技能开箱即用 (Bundled Skills)
- **自动初始化释放**：安装包完整内置 35 个 Matt Pocock AI 编程技能（TDD、代码审查、疑难诊断、架构设计、方案拷问、运维向导等），新机器启动时自动解压释放至 `~/.dsh/skills/`。
- **极简无感快捷胶囊 (Quick Action Chips)**：输入框上方提供半透明毛玻璃微标签（`🐞 诊断`、`🧪 TDD`、`🔍 审查` 等），一键快速填充技能指令。

### 4. 🤫 全静默后台与 Auto 智能模式
- **彻底消除黑框**：所有命令行工具与子进程调用强制 `windowsHide: true`，彻底告别频繁弹出的 CMD 黑色控制台。
- **Auto 智能自主模式**：内置 `@nanmicoder/dsh-auto-mode`，常规读写测试无感全自动流转，高危删库智能拦截确认。

### 5. 🔄 智能化运维与离线大模型直连
- **自动更新检查 (Auto-Updater)**：启动时静默检查 GitHub Releases，托盘支持一键手动检查最新版本。
- **离线/私有化大模型接入**：原生兼容本地 Ollama / vLLM / LM Studio，支持纯离线环境数据安全开发。
