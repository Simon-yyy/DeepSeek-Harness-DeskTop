# 全局代理规则 (AGENTS.md)

## 🚨 Git 远程推送绝对受控规则 (Strict Git Push Prohibition)

1. **严禁自动推送 (NEVER Auto-Push)**：
   - 在任何情况下，**绝对禁止自主运行或在任何构建、打包、测试脚本中自动触发 `git push`**；
   - 无论是否重新打包、更新文档、修复 Bug，在未收到用户包含明确动词（如“推送代码”、“git push”、“推送到远程”）的显式指令前，**严禁执行任何推送到 GitHub / 远程仓库的命令**。

2. **本地修改可控，远程推送必须人工确认**：
   - 允许在本地完成代码修改、编译验证与本地打包；
   - 涉及将代码同步至远端服务器时，必须先汇报修改清单与验证结果，并等待用户明确发出推送指令后再执行。

---

## 🛠️ 项目开发与验证规范

1. **知识寻路与准入链 (3-Hop Guidance)**：
   - 变更或排查时的标准寻路链：`AGENTS.md` ➔ [`contexts/context.md`](contexts/context.md) ➔ [`MEMORY.md`](MEMORY.md)（按需读取匹配卡片） ➔ 目标模块源码与单测；
   - 严禁全量盲读无关联目录、外部依赖包（`node_modules`）与构建产物目录。

2. **模块解耦与打包白名单纪律**：
   - 保持主进程轻量：主进程通用工具函数一律沉淀至 [`src/main/utils/`](src/main/utils/) 独立模块，严禁重新将通用辅助逻辑堆叠进 `main.js` 上帝文件；
   - 打包白名单联动：由于 Electron 打包采用严格白名单（`package.json` 中的 `build.files`），新增模块或资源目录必须显式确认已在白名单规则内（如 `src/**/*`），严禁遗漏导致安装包运行时丢文件。

3. **质量防线与自动化单测门禁**：
   - 零外部依赖测试：项目采用 Node.js 原生测试体系（`node:test` + `node:assert`），执行效率高且无臃肿依赖；
   - 核心逻辑门禁：凡涉及版本号判定、端口分配探测、内核大版本兼容等逻辑，必须在 [`test/`](test/) 提供测试用例；
   - 交付前验证：任何代码变更或准备打包前，必须在终端执行 `npm test`，确保 100% 绿灯且零失败方可交付。

4. **系统安全与凭据存储规范**：
   - 严禁明文凭据：任何 API Key、访问令牌等敏感数据，严禁以明文方式持久化于未受保护的本地文件；
   - 硬件级安全隔离：涉及密钥持久化时，必须通过 Electron `safeStorage` (Windows DPAPI) 加密通道写入，文件落盘必须使用 `0o600` 严格权限；
   - 网络安全与 CSP：主进程一律注入安全内容安全策略 (CSP)，严格限制非受信脚本加载；页面外链必须由系统默认浏览器打开，严禁在应用主窗口内跳转不受信外部站点。

5. **生命周期与孤儿进程守护**：
   - Windows 平台子进程防护：后台启动的深层 Node.js / NPX 服务必须在入口绑定父进程 stdin 管道 EOF 监听看门狗（如 `network-shim.js`），父进程闪退或被强杀时子进程必须在毫秒级执行 `process.exit(0)` 自毁；
   - 动态端口避让：遇到默认 3080 端口被未知进程或孤儿占用时，必须通过 `acquirePort` 自动递增避让（3081+），严禁盲目复用未知版本的监听者。

6. **代码修改与版本发布纪律**：
   - 保持代码洁癖：严禁提交临时调试代码（如未清理的 `console.log`、`debugger` 等）；
   - 严禁在工作区残留临时文件（如 `.tmp`, `.scratch`, `.bak`, `.swp` 等）；
   - 优先通过既有的 CSS 变量和轻量 DOM 注入扩展界面，避免破坏官方微内核与社区插件的基础兼容性；
   - 发版或打包请优先使用 `package.json` 中的标准命令（`npm run dist`、`npm run release` 等）；
   - 构建产物自动存放在 `release/` 目录，构建脚本已配置保留最新两个版本，勿手动破坏该清理机制。

