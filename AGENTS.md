# 全局代理规则 (AGENTS.md)

## 🚨 Git 远程推送绝对受控规则 (Strict Git Push Prohibition)

1. **严禁自动推送 (NEVER Auto-Push)**：
   - 在任何情况下，**绝对禁止自主运行或在任何构建、打包、测试脚本中自动触发 `git push`**；
   - 无论是否重新打包、更新文档、修复 Bug，在未收到用户包含明确动词（如“推送代码”、“git push”、“推送到远程”）的显式指令前，**严禁执行任何推送到 GitHub / 远程仓库的命令**。

2. **本地修改可控，远程推送必须人工确认**：
   - 允许在本地完成代码修改、编译验证与本地打包；
   - 涉及将代码同步至远端服务器时，必须先汇报修改清单与验证结果，并等待用户明确发出推送指令后再执行。
