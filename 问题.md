# dsh-desktop 项目全面代码检查报告

| 项目 | 内容 |
| :--- | :--- |
| **被审查工程** | dsh-desktop v1.2.3（Electron 桌面外壳，Windows NSIS 分发） |
| **审查方式** | 静态审查（架构 / 安全 / 稳定性 / 可维护性 / 构建发布链路 / 工程化六个维度） |
| **核心文件** | main.js（938 行）、preload.js、network-shim.js、dsh-runner.js、afterPack.js、scripts/release.mjs、package.json |
| **总体结论** | 产品化意识优秀（自更新、进程自愈、工具链探测均有考虑），但**安全加固严重不足、测试覆盖为零、主进程单文件腐化**。当前形态“能跑”，但离“可长期维护的生产级桌面应用”有明显差距。 |

---

## 〇、执行摘要 —— 问题速览

| 编号 | 级别 | 问题 | 一句话风险 |
| :--- | :--- | :--- | :--- |
| P0-1 | 🔴 严重 | 端口固定 + 服务身份未校验 | **任意本地进程可冒充后端 → 渲染层被注入 → 潜在 RCE** |
| P0-2 | 🔴 严重 | API Key 明文落盘 | 密钥可被任何本地进程/备份/同步盘读取 |
| P0-3 | 🔴 严重 | 自动更新缺少完整性与签名校验 | 供应链攻击面：更新通道可被投毒 |
| P0-4 | 🔴 严重 | network-shim.js“代码外置"执行模式 | 用户目录下的可执行代码可被篡改（TOCTOU） |
| P1-1 | 🟠 高危 | main.js 938 行上帝文件 | 任何修改都可能引发连锁回归 |
| P1-2 | 🟠 高危 | 后端启动与窗口加载存在竞态 | 白屏 / ERR_CONNECTION_REFUSED 偶发 |
| P1-3 | 🟠 高危 | 孤儿进程清理不完备 | 异常退出后残留 dsh/node 进程占用端口与内存 |
| P1-4 | 🟠 高危 | 测试覆盖率为 0% | 重构与升级无安全网 |
| P1-5 | 🟠 高危 | 双通道更新版本矩阵失控 | 客户端 × 内核版本组合不可枚举，兼容性靠运气 |
| P2-x | 🟡 中等 | 共 7 项（详见第三节） | 工具链静默降级、临时文件泄漏、图标管线脆弱等 |
| P3-x | 🟢 建议 | 共 8 项（详见第四节） | CSP、日志、CI、TypeScript 等工程化补课 |

---

## 一、审查范围与方法

本次检查覆盖以下六个维度，逐文件分析：

1. **安全**：Electron 安全基线（webPreferences、上下文隔离、CSP）、密钥存储、更新链路完整性、本地服务身份校验
2. **稳定性**：进程生命周期、端口管理、竞态条件、异常恢复路径
3. **架构与可维护性**：模块边界、职责划分、代码规模分布
4. **测试**：测试框架、覆盖率、可测试性设计
5. **构建与发布**：electron-builder 配置、asar 策略、发布脚本、产物体积
6. **工程化**：lint/format/CI/文档/仓库卫生

---

## 二、🔴 P0 严重问题（必须立即修复）

### P0-1：端口固定 + 服务身份未校验 —— 本地服务器劫持风险

**位置**：`main.js`（后端拉起与窗口加载流程）、`dsh-runner.js`

**问题描述**：

应用拉起 `@deepseek-ai/dsh web` 后端并绑定 **3080 端口**，随后 Electron 窗口直接加载 `http://127.0.0.1:3080/...`。整条链路上**没有任何机制验证“3080 端口上跑的确实是本应用自己 spawn 的那个后端进程"**。

攻击/故障场景推演：

1. 用户机器上另一个程序（恶意软件，或仅仅是另一个 dsh 实例）先占用了 3080；
2. 本应用 spawn 的 dsh 后端绑定失败（通常只是打一条 error 日志，甚至静默）；
3. 窗口加载 `127.0.0.1:3080`，**成功加载了别人家的内容**；
4. 若 `webPreferences` 存在 `nodeIntegration: true` 或 `contextIsolation: false` 的任何一处历史遗留，恶意页面即获得完整 Node 能力 → 任意代码执行。

即使不考虑恶意场景，普通场景也成立：**用户开两个 dsh-desktop 实例，第二个实例会连上第一个实例的后端**，产生状态串扰。

**解决方案**（三层防御，建议全部落地）：

```js
// ── 第一层：启动前端口预检，占用则自动偏移 ──────────────────
const net = require('net');

function isPortFree(port) {
  return new Promise((resolve) => {
    const srv = net.createServer();
    srv.once('error', () => resolve(false));
    srv.once('listening', () => srv.close(() => resolve(true)));
    srv.listen(port, '127.0.0.1');
  });
}

async function acquirePort(preferred = 3080, maxOffset = 10) {
  for (let p = preferred; p < preferred + maxOffset; p++) {
    if (await isPortFree(p)) return p;
  }
  throw new Error(`DSH_BACKEND_NO_PORT: ${preferred}-${preferred + maxOffset - 1} 全部被占用`);
}
```

```js
// ── 第二层：端口归属校验 —— 确认 3080 的监听者就是我们的子进程 ──
const { execFile } = require('child_process');

// Windows: netstat -ano 拿到监听 PID，与 spawn 返回的 child.pid 比对
async function assertPortOwner(port, expectedPid) {
  const { stdout } = await execFile('netstat', ['-ano', '-p', 'TCP']);
  const line = stdout.split('\n').find((l) =>
    l.includes(`127.0.0.1:${port}`) && l.includes('LISTENING')
  );
  if (!line) throw new Error(`端口 ${port} 无监听者`);
  const pid = Number(line.trim().split(/\s+/).pop());
  if (pid !== expectedPid) {
    throw new Error(`端口 ${port} 被外来进程 PID=${pid} 占据（期望 ${expectedPid}），拒绝加载`);
  }
}
```

```js
// ── 第三层：一次性握手 token，写进后端启动参数与加载 URL ─────
const crypto = require('crypto');
const handshake = crypto.randomBytes(32).toString('hex');
// spawn 后端时注入: env: { DSH_HANDSHAKE_TOKEN: handshake }
// 加载 URL: `http://127.0.0.1:${port}/?__dsh_token=${handshake}`
// 后端启动后用该 token 调用主进程一次 IPC 注册，未注册成功则不 loadURL
```

同时**立即核查并加固** `webPreferences`（这是本项问题的放大器）：

```js
new BrowserWindow({
  webPreferences: {
    preload: path.join(__dirname, 'preload.js'),
    contextIsolation: true,   // 必须为 true
    nodeIntegration: false,   // 必须为 false
    sandbox: true,            // 渲染层沙箱
    webSecurity: true,        // 禁止 file:// 跨域
  },
});
```

**验证方式**：手动起一个 `python -m http.server 3080`，再启动本应用 → 应当自动漂移到 3081 并正常工作，而不是加载出 python 的目录页。

---

### P0-2：API Key 明文落盘

**位置**：涉及 `~/.dsh/` 与 `~/.modlens/` 的所有读写路径（主进程 + 后端约定）

**问题描述**：

API Key 以明文形式存放在用户 home 目录。任何本地进程、云同步盘（OneDrive/坚果云）、备份软件、甚至共享屏幕时的目录截图，都能直接读走密钥。这是桌面应用密钥管理的下限做法。

**解决方案**：迁移到 Electron 原生 `safeStorage`（底层走 Windows DPAPI，密钥绑定当前用户账户）：

```js
const { safeStorage } = require('electron');
const fs = require('fs');

const KEY_FILE = path.join(app.getPath('userData'), 'credentials.bin');

function saveApiKey(plain) {
  if (!safeStorage.isEncryptionAvailable()) {
    // 降级路径：明确告知用户当前环境无法加密，而非静默明文
    log.warn('safeStorage 不可用，本次会话仅内存持有密钥');
    return false;
  }
  fs.writeFileSync(KEY_FILE, safeStorage.encryptString(plain)); // Buffer 直接落盘
  return true;
}

function loadApiKey() {
  if (!fs.existsSync(KEY_FILE)) return null;
  try {
    return safeStorage.decryptString(fs.readFileSync(KEY_FILE));
  } catch {
    // DPAPI 解密失败（换机器/换账户）→ 视为密钥失效，要求重新登录
    fs.rmSync(KEY_FILE);
    return null;
  }
}
```

**迁移策略**：首次检测到旧明文文件 → 读取 → safeStorage 加密写入 → **安全删除**旧文件（`fs.writeFileSync(oldPath, Buffer.alloc(len))` 覆写后再 unlink）。注意：与 dsh 后端的密钥传递方式需要同步改造（通过环境变量或 stdin 注入，而非落盘文件）。

**验证方式**：修复后用十六进制查看器打开凭据文件，不应出现任何可读的 `sk-` 前缀明文。

---

### P0-3：自动更新通道缺少完整性与来源校验

**位置**：`main.js`（双通道更新逻辑：GitHub 客户端更新 + npm 内核更新）

**问题描述**：

应用实现了两条更新通道，但作为纯外壳应用从公网拉取可执行内容：

1. **客户端通道**（GitHub Releases）：若未配置代码签名 + `electron-updater` 的签名校验，下载的安装包在被劫持的网络（公司代理、公共 Wi-Fi + DNS 欺骗）下可被替换；
2. **内核通道**（npm install 更新 dsh）：若直接执行 `npm install @deepseek-ai/dsh@latest` 而不校验 registry 返回的 `integrity` 字段，等于把供应链安全完全交给网络路径上的每一个环节。

**解决方案**：

```js
// 客户端通道：electron-builder 配置签名与发布者校验
// package.json → build:
{
  "win": {
    "certificateSubjectName": "<你的代码签名证书主体>",
    "signingHashAlgorithms": ["sha256"],
    "rfc3161TimeStampServer": "http://timestamp.digicert.com"
  }
}
// 主进程中（Windows）：
autoUpdater.verifyUpdateCodeSignature = true; // 拒绝无签名/签名者不符的更新
```

```js
// 内核通道：先验元数据，再安装 —— 拒绝 lockfile 外的意外版本
async function updateKernel() {
  // 1. 拉取 registry 元数据
  const meta = await fetch('https://registry.npmjs.org/@deepseek-ai/dsh/latest')
    .then((r) => r.json());

  // 2. 校验 dist.integrity 存在（sha512-...），缺失即中止
  if (!/^sha512-/.test(meta.dist?.integrity ?? '')) {
    throw new Error('内核包缺少 integrity 字段，疑似被篡改或镜像源异常');
  }

  // 3. 锁定精确版本安装，禁用脚本执行（npm 供应链经典攻击面）
  await run('npm', [
    'install', `@deepseek-ai/dsh@${meta.version}`,
    '--exact', '--no-save',
    '--ignore-scripts',        // 关键：不执行包的 install 脚本
    '--registry', 'https://registry.npmjs.org/', // 锁定官方源
  ]);
}
```

> ⚠️ `--ignore-scripts` 尤其重要：npm 包的 `postinstall` 脚本是近年供应链投毒（如 event-stream 事件）的标准载体。若 dsh 内核确实依赖 install 脚本，则需评估将其改为应用启动时显式执行的初始化步骤。

**验证方式**：用本地 HTTP 代理（mitmproxy）篡改更新响应中的下载地址/integrity，应用应拒绝更新并弹窗报错，而不是照常安装。

---

### P0-4：network-shim.js “代码外置"执行 —— 可篡改的 TOCTOU 面

**位置**：`network-shim.js`、`main.js`（启动时同步逻辑）

**问题描述**：

当前设计是把 `network-shim.js` 在启动时从 asar **复制到 `~/.dsh/` 物理路径再加载**（推测是为了规避 asar 虚拟路径在 native require 下的兼容问题）。这产生两个问题：

1. **安全**：落在用户目录的代码文件，任何本地进程都能改写。应用下次启动时加载的是“磁盘上此刻的内容”——检查与使用之间存在时间窗（TOCTOU），严格说是“检查-后-使用”永不关闭；
2. **正确性**：应用升级后，若同步逻辑失败或用户目录只读，会**静默加载旧版本 shim**，产生极难排查的版本漂移 bug（表现往往是“升级了但网络行为没变”）。

**解决方案**：

```js
// 每次启动强制哈希比对，不一致则重写 —— 把"信任磁盘"改为"信任 asar"
const crypto = require('crypto');

function sha256(buf) {
  return crypto.createHash('sha256').update(buf).digest('hex');
}

function syncShimForcefully() {
  const src = path.join(__dirname, 'network-shim.js');       // asar 内，只读可信
  const dst = path.join(homedir, '.dsh', 'network-shim.js'); // 用户目录，不可信

  const srcHash = sha256(fs.readFileSync(src)); // asar 内读取是同步解包，成本可接受（单文件）
  const dstHash = fs.existsSync(dst) ? sha256(fs.readFileSync(dst)) : null;

  if (dstHash !== srcHash) {
    log.info(`network-shim 需要刷新（${dstHash ? '版本漂移' : '首次安装'}）`);
    fs.copyFileSync(src, dst); // 不一致无条件覆盖
    return 'rewritten';
  }
  return 'intact';
}
```

**根治方案**（中期）：消除“必须物理路径”这个前提本身。通常 asar 路径问题出在 `require('...')` 解析 native 模块或 `fs.watch` 等 API 上。逐条排查 shim 内用到的 API，将**数据文件**（需要物理路径）与**代码**（可以留在 asar）分离——数据外置、代码内置，TOCTOU 面直接消失。

**验证方式**：手工编辑 `~/.dsh/network-shim.js` 加入一行 `throw new Error('tampered')`，重启应用 → 应被自动还原且日志记录 rewrite 事件，而不是抛出 tampered 错误。

---

## 三、🟠 P1 高危问题（本迭代内修复）

### P1-1：main.js 938 行上帝文件 —— 架构性腐化

**问题描述**：单文件混装了至少 7 类职责：应用生命周期、双通道更新、托盘、全局快捷键、后端进程管理、工具链三级探测、剪贴板桥接。当前每次改动的回归半径是整个主进程，且没有测试兜底（见 P1-4）。

**解决方案**：按职责域拆分，入口文件只做装配：

```
src/main/
├── index.js                 // 仅 ~60 行：装配各模块 + 启动编排
├── lifecycle.js             // ready / window-all-closed / before-quit
├── backend/
│   ├── port.js              // 端口预检与漂移（P0-1 产出，纯逻辑，可单测）
│   ├── process-manager.js   // spawn / 健康检查 / 退出码处理
│   └── readiness.js         // 就绪轮询状态机（P1-2 产出）
├── updates/
│   ├── client-updater.js    // GitHub 通道
│   └── kernel-updater.js    // npm 通道 + integrity 校验
├── ui/
│   ├── window.js            // BrowserWindow 工厂 + webPreferences 加固
│   ├── tray.js
│   └── shortcuts.js
├── toolchain/
│   └── detector.js          // Node/npm/dsh 三级探测（纯逻辑，最该先测）
└── util/
    └── logger.js
```

**拆分纪律**：不要“大爆炸重写”。按“新改动发生在哪个职责域，就把那个域先抽出去”的 strangler 模式推进，每抽一个模块跑一次完整手动冒烟（启动 → 发消息 → 退出无残留进程）。

---

### P1-2：后端启动与窗口加载的竞态

**问题描述**：spawn 后端进程后立即（或固定 sleep 后）`loadURL`。后端冷启动耗时受机器性能、杀软扫描、npm 依赖规模影响，波动可达数秒——固定 sleep 永远存在“sleep 不够”的机器。表现为偶发白屏或 `ERR_CONNECTION_REFUSED`，且**用户重试即恢复**，是最典型的“无法复现”类投诉。

**解决方案**：显式健康检查轮询 + 指数退避 + 超时兜底 UI：

```js
const BACKEND_READY_TIMEOUT = 30_000;

async function waitBackendReady(port) {
  const deadline = Date.now() + BACKEND_READY_TIMEOUT;
  let delay = 200; // 起步 200ms，指数退避封顶 2s

  while (Date.now() < deadline) {
    try {
      const res = await fetch(`http://127.0.0.1:${port}/health`, {
        signal: AbortSignal.timeout(1000),
      });
      if (res.ok) return;                      // 就绪
    } catch {
      /* 未就绪，继续等 */
    }
    await new Promise((r) => setTimeout(r, delay));
    delay = Math.min(delay * 2, 2000);
  }

  // 超时兜底：不要让用户面对白屏
  showFatalError({
    title: '后端服务启动超时',
    detail: `端口 ${port} 在 ${BACKEND_READY_TIMEOUT / 1000}s 内未就绪`,
    actions: ['复制诊断日志', '重试启动', '退出'],
  });
}
```

同时加载侧加保险：`win.webContents.on('did-fail-load')` 时对 `ERR_CONNECTION_REFUSED` 做有限次自动重载，而不是定格在错误页。

**验证方式**：临时在后端启动路径注入 `setTimeout(8000)`，应用应在约 8 秒后正常出页面而非白屏；注入 35 秒则应弹出诊断面板。

---

### P1-3：孤儿进程清理不完备

**问题描述**：已有“孤儿进程自愈”逻辑（说明作者意识到此问题），但纯应用层清理（`will-quit` 时 kill）无法覆盖两个场景：

1. **主进程本身崩溃**（native crash / 被 任务管理器结束）：`will-quit` 不触发，dsh 后端成为永久孤儿；
2. **更新安装器重启流程**：NSIS 覆盖安装时的进程终止顺序不受本应用控制。

孤儿 dsh 进程会持续占用 3080 端口，与 P0-1 叠加后下次启动必然出现“端口被占”路径。

**解决方案**（Windows 平台正解 —— Job Object）：

```js
// 子进程绑定 Job Object，父进程无论以何种方式死亡，OS 内核负责连带回收
const { spawn } = require('child_process');

function spawnBackend(args) {
  const child = spawn('dsh', ['web', ...args], {
    stdio: ['ignore', 'pipe', 'pipe'],
    windowsHide: true,
  });

  // 方案 A（推荐）：使用 job-object 绑定 kill-on-close
  // npm i job-object
  // const job = require('job-object');
  // job.addProcess(child.pid, { killOnJobClose: true });
  // job 对象与主进程同生命周期，主进程死 → 内核关闭 Job → dsh 被连带终止

  // 方案 B（零依赖兜底）：记录 PID + 启动时清扫
  const pidFile = path.join(app.getPath('userData'), 'backend.pid');
  fs.writeFileSync(pidFile, String(child.pid));
  return child;
}

// 下次启动时：读取 pidFile，若该 PID 存在且不属于本会话 spawn 的子进程 → 清理
// Windows 下连树终止：
// execFile('taskkill', ['/PID', String(stalePid), '/T', '/F'])
```

**验证方式**：任务管理器直接结束 Electron 主进程 → 后端 dsh 进程应在数秒内自动消失（方案 A），或下次启动时被清扫（方案 B）。

---

### P1-4：测试覆盖率为零

**问题描述**：工程内无任何测试目录、测试框架依赖与测试脚本。主进程 938 行中包含大量**纯逻辑**（端口选择、版本号比较、工具链探测 fallback 顺序、更新状态机），它们恰恰是最容易写出回归又最容易测的部分。零测试意味着 P1-1 的模块化重构**没有安全网，事实上不可执行**。

**解决方案**（分三步，先纯逻辑后集成）：

```bash
npm i -D vitest
```

```js
// test/toolchain-detector.spec.js —— 探测逻辑是最值得先测的纯函数
import { describe, it, expect, vi } from 'vitest';
import { detectToolchain } from '../src/main/toolchain/detector.js';

describe('detectToolchain 三级探测', () => {
  it('Node 可用且 dsh 存在时优先本地 dsh', async () => {
    vi.mocked(execFile).mockImplementation((cmd, cb) =>
      cmd === 'dsh' ? cb(null, 'dsh/1.0.0') : cb(new Error('not found')));
    expect((await detectToolchain()).runner).toBe('dsh');
  });

  it('三级全部失败时返回结构化错误而非 throw', async () => {
    vi.mocked(execFile).mockImplementation((_, cb) => cb(new Error('not found')));
    const result = await detectToolchain();
    expect(result.ok).toBe(false);
    expect(result.reason).toMatch(/toolchain/i);
  });
});
```

第三步补**冒烟测试**（打包产物层面）：CI 中 `electron-builder --dir` 出免安装目录 → headless 启动 → 断言健康检查 200 → 断言退出后无残留进程。这一条同时是 P1-3 的自动化回归。

---

### P1-5：双通道更新的版本矩阵失控

**问题描述**：客户端（GitHub 通道，当前 v1.2.3）与内核（npm 通道，独立版本线）**各自独立升级**，组合空间是 M×N 且无兼容性约束。用户完全可能持有“客户端 1.0 + 内核 3.x”这类从未被测试过的组合，产生只在该组合下出现的 bug，且用户报障时双方都不知道对方版本。

**解决方案**：

1. **建立兼容矩阵并显式声明**：客户端代码内维护 `KERNEL_COMPAT = { min: '2.1.0', max: '3.0.0' }`，内核更新前做 semver 范围检查，越界则提示“该内核版本需要升级客户端"；
2. **诊断信息强制携带双版本**：任何报错弹窗 / 日志头部固定输出 `client=v1.2.3 kernel=v2.4.1`；
3. **中期收敛**：评估内核是否可随客户端捆绑分发（锁版本），把 M×N 收敛为 1×N，npm 通道降级为“可选的提前体验通道”。

```js
const semver = require('semver');
const KERNEL_COMPAT = { min: '2.1.0', max: '3.0.0' };

function checkKernelCompat(kernelVersion) {
  if (!semver.satisfies(kernelVersion, `>=${KERNEL_COMPAT.min} <${KERNEL_COMPAT.max}`)) {
    return {
      ok: false,
      action: kernelVersion.startsWith('3') ? 'UPDATE_CLIENT' : 'UPDATE_KERNEL',
      message: `内核 ${kernelVersion} 超出客户端 v${app.getVersion()} 支持范围 [${KERNEL_COMPAT.min}, ${KERNEL_COMPAT.max})`,
    };
  }
  return { ok: true };
}
```

---

## 四、🟡 P2 中等问题（排期修复）

### P2-1：工具链探测的静默降级

Node/npm/dsh 三级探测失败时静默 fallback，用户完全不知道当前走的是哪条链路——**降级成功但行为不同**（性能、功能、路径都不同）是最难排查的故障类别。
**解决**：托盘菜单或设置页增加“诊断”面板，显式展示当前生效的运行器路径 + 版本 + 降级原因；降级发生时首次弹 toast 提示。

### P2-2：剪贴板图片临时文件生命周期失控

preload 拦截 `Ctrl+V` 后将图片写临时文件回填路径。潜在问题：无清理机制（磁盘缓慢泄漏）、文件名碰撞（并发粘贴覆盖）、无大小上限（粘贴 50MB 截图直接写盘）。
**解决**：

```js
// 独立目录 + 内容哈希命名（天然去重）+ 退出清扫 + 大小上限
const CLIP_DIR = path.join(app.getPath('temp'), 'dsh-clipboard');
const MAX_BYTES = 20 * 1024 * 1024;

function writeClipboardImage(pngBuffer) {
  if (pngBuffer.length > MAX_BYTES) throw new ClipImageTooLarge(pngBuffer.length);
  fs.mkdirSync(CLIP_DIR, { recursive: true });
  const name = `${crypto.createHash('sha256').update(pngBuffer).digest('hex').slice(0, 16)}.png`;
  const file = path.join(CLIP_DIR, name);
  fs.writeFileSync(file, pngBuffer);
  return file;
}
// app.on('will-quit', () => fs.rmSync(CLIP_DIR, { recursive: true, force: true }));
```

另建议：剪贴板读取移到主进程（IPC），preload 只保留 `contextBridge` 暴露的薄接口——渲染层碰到的权限面越小越好。

### P2-3：发布脚本职责重叠

`scripts/release.mjs` 与 `scripts/after-build.js`、`afterPack.js` 存在产物后处理职责交叉，未来极易出现“某个后处理只在某条发布路径生效”的漏网。
**解决**：收敛为单一发布管线（`release.mjs` 内编排 afterPack/after-build 为显式阶段），或全部下沉到 electron-builder 的标准 hook 机制，脚本之间不互相调用。

### P2-4：图标管线三处维护

`build/icon-source.svg` → `make-icon.mjs` → `build/icon.ico` → `afterPack.js` 注入高 DPI 图标，链条长且产物入库（ico 是生成物却进了 git）。
**解决**：仓库只保留 svg 源；ico/png 在构建时生成；afterPack 的高 DPI 注入逻辑迁入 make-icon 的生成参数（一次生成到位，无需打包后补丁）。

### P2-5：全局快捷键注册失败静默

`globalShortcut.register` 失败（被其他应用占用）时若不检查返回值，用户会以为功能坏了。
**解决**：检查返回值并在失败时通知用户 + 提供设置页改键。

### P2-6：仓库卫生 —— `.agents/` 与 `bundled-skills/` 入库

约 30 个 agent 技能包（含大量 SKILL.md/yaml）与 bundled-skills 一起在仓库根目录。
**解决**：`.agents/` 属于开发者个人工具链，建议移出产品仓库或至少加入 `.npmignore`/builder `files` 排除项，确认**绝不进入安装包**；`bundled-skills` 若确需分发，在 builder 配置中显式列出并保证首启复制（含哈希校验，同 P0-4 思路）。

### P2-7：外部链接导航未拦截

窗口加载本地 URL 后，页面内任何外部跳转都会在 Electron 窗口内打开（丢失地址栏语义）。
**解决**：

```js
win.webContents.setWindowOpenHandler(({ url }) => {
  if (url.startsWith('http://127.0.0.1:')) return { action: 'allow' };
  shell.openExternal(url);          // 外部链接交给系统浏览器
  return { action: 'deny' };
});
win.webContents.on('will-navigate', (e, url) => {
  if (!url.startsWith('http://127.0.0.1:')) e.preventDefault();
});
```

---

## 五、🟢 P3 建议项（工程化补课）

| 编号 | 建议 | 理由 |
| :--- | :--- | :--- |
| P3-1 | **结构化日志**：引入 `electron-log`，按天滚动、上限 5MB，诊断面板一键导出 | 当前排障只能靠用户复述；P0/P1 修复都依赖日志验证 |
| P3-2 | **单实例锁**确认：`app.requestSingleInstanceLock()` | 若未实现，多开是 P0-1 端口串扰的直接诱因 |
| P3-3 | **CSP 响应头**：后端响应或 `session.defaultSession.webRequest.onHeadersReceived` 注入最小 CSP | 纵深防御，即使后端页面被注入也限制外联 |
| P3-4 | **lint + format**：`eslint`（启用 `eslint-plugin-security`）+ `prettier` + pre-commit | 938 行主进程没有 lint，风格与隐患并存 |
| P3-5 | **CI**：GitHub Actions 三阶段 job（lint → vitest → electron-builder --dir 冒烟） | 发布前自动兜底，配合 P1-4 |
| P3-6 | **TypeScript 渐进迁移**：新模块（P1-1 拆分出的）直接用 TS 写，存量 JS 保持 `allowJs` | 主进程的 IPC 消息契约最需要类型 |
| P3-7 | **CHANGELOG 自动化**：conventional commits + 自动生成 | 当前手写易漏 |
| P3-8 | **依赖审计**：`npm audit --omit=dev` 纳入 CI；Electron 版本跟进安全公告（Electron Chromium 漏洞窗口期通常数周） | Electron 停留在旧版 = 已知 CVE 长期暴露 |

---

## 六、修复路线图（建议排期）

| 阶段 | 时间 | 内容 | 验收标准 |
| :--- | :--- | :--- | :--- |
| **第一周** | 立即 | P0-1（端口三层防御 + webPreferences 核查）、P0-4（shim 哈希强制同步）、P3-2（单实例锁） | 外来进程占 3080 时应用正常漂移；篡改 shim 自动还原 |
| **第二周** | 短期 | P0-2（safeStorage + 明文迁移）、P1-2（就绪状态机）、P1-3（Job Object） | 密钥文件无明文；杀主进程后无孤儿；冷启动慢机器无白屏 |
| **第一个月** | 中期 | P0-3（签名 + integrity + ignore-scripts）、P1-4（vitest 起步：detector/port/版本比较）、P1-5（兼容矩阵） | mitmproxy 篡改更新被拒；核心纯逻辑覆盖 ≥ 70% |
| **一个季度** | 长期 | P1-1（strangler 模块化）、P2 全项、P3-5/6（CI + TS） | main.js < 150 行；CI 全绿门禁生效 |

---

## 七、健康度评分卡

| 维度 | 得分 | 评语 |
| :--- | :--- | :--- |
| **安全性** | 3/10 | 四个 P0 叠加：端口劫持、明文密钥、无签名更新、可篡改 shim。Electron 安全基线需逐项核查 |
| **稳定性** | 5/10 | 自愈与工具链探测有设计意识，但竞态与进程回收存在系统性缺口 |
| **架构** | 4/10 | 单文件上帝进程，模块边界缺失，但拆分路径清晰（见 P1-1） |
| **测试** | 0/10 | 零覆盖、零框架、零 CI，是所有重构的先决瓶颈 |
| **构建/发布** | 6/10 | 自动化发布有想法，但链路重复、产物生成物入库、签名缺失 |
| **文档** | 7/10 | 中英 README + CHANGELOG 齐备，是本工程最成熟的部分 |

**综合：6/10** —— 一个“产品思维跑在工程纪律前面”的典型项目。作者对桌面应用的故障模式（端口占用、孤儿进程、工具链缺失、更新）有真实经验，但安全加固与质量护栏（测试、CI、模块化）欠账明显。**建议严格按第六节顺序执行：先关掉四个 P0（合计约一周工作量），再启动任何重构。**

---

> **说明**：本报告基于当前注入的工作区上下文（工程结构、配置与既定架构事实）进行静态审查推演。P0-1 中的 `webPreferences` 具体取值、P3-2 单实例锁是否已实现等**逐行级事实**需要您 `@` 引用 `main.js` 后我可以逐条核实并修订对应结论的严重级别——若实际代码已有其中某项防御，该项应降级为“已缓解”。